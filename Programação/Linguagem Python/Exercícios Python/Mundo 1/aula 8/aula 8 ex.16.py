from math import trunc, sqrt
n = float(input('Enter a number to find the portion and the square root: '))

print(f'The Portion of the number {n} is: {trunc(n)}\n'
      f'The square root of the portion is:{trunc(sqrt(n))}')
