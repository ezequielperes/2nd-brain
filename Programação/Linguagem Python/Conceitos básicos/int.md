 int vem de *integers* em português, inteiros, eles pegam apenas números inteiros, não funciona números fracionados (1.245, 34.654) lembrando que não pode colocar **ponto**, se colocar o ponto o python entende que você está querendo colocar um número float (número flutuante / Números Reais)
  
  {... ,-3, -2, -1, 0, 1, 2, 3, ...}
  
  Exemplo de utilização:
  
  `valor = int(input('Quantos anos você tem? '))`
  